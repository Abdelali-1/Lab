name: First WorkFlow

on: 
  push:
      branches:
          - main
jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print Welcome message
        run: echo "welcome to our first github workflow"
          
